{
  "mcpServers": {
    "probors": {
      "command": "npx",
      "args": [
        "mcp-remote",
        "https://probors-service.bc-smart.com/mcp",
        "--header",
        "Authorization: Bearer ${PROBORS_API_KEY}"
      ]
    }
  }
}
